# movies/repositories/movie_repository.py
from typing import Iterable
# from movies.models import Movie  # importera quand le modèle existera

class MovieRepository:
    # squelette — on remplira à l’étape modèles
    @staticmethod
    def list_all() -> Iterable:
        # return Movie.objects.all()
        return []